@model List<Category>



<ul class="portfolio-filters isotope-filters" data-aos="fade-up" data-aos-delay="100">
    <li data-filter="*" class="filter-active">All</li>
    @foreach (var x in Model)
    {
    <li data-filter=".filter-@x.CategoryId">@x.CategoryName</li>
     }
</ul>
